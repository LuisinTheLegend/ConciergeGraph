================================================================================
PLANO DETALHADO — STRESS TEST COMPLETO v2 (Grafo Concierge v3.8.0)
================================================================================

OBJETIVO: Testar TODAS as camadas do sistema de ponta a ponta, sem exceções.
O teste anterior (memory_stress_test.py) cobria apenas 4 dimensões básicas
(Ingestão, Hierarquia, Janitor, Decaimento). Este novo teste cobre 8 dimensões
incluindo as novas camadas core/, agents/ e interface/.

ARQUIVO: tests/stress_test_v2.py
EXECUÇÃO: python -m tests.stress_test_v2

================================================================================
ESTRUTURA DO ARQUIVO
================================================================================

O arquivo será dividido em 3 partes para evitar estouro de tokens:
  - tests/stress_test_v2_parte1.py  → Setup + Dimensões 1-3 (Storage/Ingestão/Core)
  - tests/stress_test_v2_parte2.py  → Dimensões 4-6 (Agents/Interface/Hooks)
  - tests/stress_test_v2_parte3.py  → Dimensões 7-8 (Concorrência/Edge Cases) + Main

================================================================================
SETUP (Comum a todos os testes)
================================================================================

1. Workspace:
   - Diretório: _stress_test_v2_workspace/
   - Cria 3 "projetos fake" com arquivos .py, .md, .json, .gitignore
   - Projeto A: "fintech-api" (wing="finanças/quant", privacy=INTERNAL)
     - src/trading.py (classe TradingEngine com 5 métodos)
     - src/risk.py (classe RiskCalculator)
     - src/utils.py (funções auxiliares)
     - README.md, package.json, .gitignore
   - Projeto B: "marketing-dashboard" (wing="marketing/vendas", privacy=PUBLIC)
     - src/analytics.py (classe AnalyticsTracker)
     - src/campaigns.py (classe CampaignManager)
     - README.md
   - Projeto C: "vault-secrets" (wing="gestão/saas", privacy=RESTRICTED)
     - src/vault.py (classe SecretVault)
     - README.md

2. Bootstrap:
   - SqliteStore (DB temporário)
   - ChromaVectorStore (coleção temporária)
   - EmbeddingManager (tier=FLASH)
   - LLMAdapter (fake fallback se sem API key)
   - ZoomSummarizer
   - IngestionManager
   - GrafoConcierge (Fachada Central)
   - RevisorCritico (modo heurístico)
   - ActionHooks
   - JanitorService

3. Helpers:
   - report(label, passed, detail) → registra resultado
   - header(title) → imprime cabeçalho visual
   - _results: list[tuple] → acumulador global

================================================================================
DIMENSÃO 1: STORAGE (SqliteStore + ChromaVectorStore)
================================================================================

Testa a camada de persistência isoladamente.

TEST 1.1 — CRUD de Projetos
  - create_project() para os 3 projetos
  - get_project() por UUID → valida campos retornados
  - get_project() por folder_name → valida lookup alternativo
  - list_projects() → deve retornar 3
  - update_project() → muda summary de um projeto
  - Verifica que privacy_level está persistido corretamente

TEST 1.2 — CRUD de Nós
  - create_node() com cada node_type (FACT, SKILL, INSIGHT, TRAJECTORY, PATCH)
  - get_node() → valida que retorna os campos corretos
  - get_nodes_by_project() → filtra por projeto
  - get_nodes_by_project(status="ACTIVE") → filtra por status
  - update_node(status="ARCHIVED") → muda status
  - find_node_by_hash() → busca por hash de arquivo
  - touch_node_commit() → atualiza last_commit_at

TEST 1.3 — Arestas (Grafo)
  - create_edge() entre nós do mesmo projeto
  - get_edges_from(node_id) → arestas de saída
  - get_edges_to(node_id) → arestas de entrada
  - get_in_degree(node_id) → contagem de entrada
  - delete_edge() → remove uma aresta
  - get_dependency_tree() → árvore CTE recursiva
  - get_reverse_dependency_tree()

TEST 1.4 — Commits
  - create_commit() com todos os campos
  - get_recent_commits(limit=3) → verifica ordem DESC
  - get_last_commit_phase() → retorna a fase do último commit

TEST 1.5 — Trajetórias Episódicas
  - create_trajectory() com erro e solução
  - get_trajectories(project_uuid) → lista trajetórias
  - get_trajectories(status="ACTIVE") → filtra

TEST 1.6 — FTS5 (Full-Text Search)
  - fts_search("trading engine") → deve encontrar nós do fintech-api
  - fts_search("campaign") → deve encontrar nós do marketing
  - fts_search("inexistente_xyz") → deve retornar lista vazia
  - fts_rebuild() → reconstrua o índice sem erro

TEST 1.7 — ChromaDB
  - Armazena 10 embeddings via vector_store.store_batch()
  - vector.count() → deve ser >= 10
  - vector.search(query_embedding, project_uuids) → retorna resultados
  - vector.delete_batch() → remove embeddings específicos
  - vector.count() → deve ter diminuído

TEST 1.8 — Estatísticas
  - get_project_stats() → valida total_nodes, total_edges, etc.
  - get_reference_wings() → retorna lista (pode estar vazia)

CRITÉRIOS DE SUCESSO: 100% dos testes devem passar.

================================================================================
DIMENSÃO 2: INGESTÃO (IngestionManager + ZoomSummarizer)
================================================================================

Testa o pipeline de ingestão de ponta a ponta.

TEST 2.1 — Mine do Projeto A (fintech-api)
  - manager.mine(project_uuid_A, path_A, auto_tag=True)
  - Valida: files_processed >= 4 (trading, risk, utils, README)
  - Valida: nodes_created > 0
  - Valida: embeddings_stored > 0
  - Valida: files_skipped >= 1 (.gitignore)

TEST 2.2 — Mine do Projeto B (marketing-dashboard)
  - Mesma validação mas para projeto B

TEST 2.3 — Re-mine (Idempotência)
  - Executa mine() novamente no Projeto A SEM alterar arquivos
  - Valida: files_processed = 0 ou nodes_created = 0 (hash check previne duplicação)

TEST 2.4 — Mine após Modificação
  - Altera o conteúdo de trading.py (adiciona um método novo)
  - Executa mine() novamente
  - Valida: detectou a mudança e atualizou o nó

TEST 2.5 — Mine após Deleção
  - Deleta risk.py do filesystem
  - Executa mine() novamente
  - Valida: files_deleted >= 1

TEST 2.6 — Hierarquia Zoom (L1/L2)
  - generate_project_context(project_uuid_A)
  - Valida: l1_count > 0 (clusters de pasta)
  - Valida: l2_summary não vazio (Bússola)

CRITÉRIOS DE SUCESSO: 100% dos testes devem passar.

================================================================================
DIMENSÃO 3: CORE (Config + ProjectIndex + HybridSearch + Middleware)
================================================================================

Testa as novas camadas construídas nas Fases 2-4.

TEST 3.1 — ConciergeConfig
  - Instancia DEFAULT_CONFIG
  - Valida pesos: w_vetorial=0.50, w_frequencia=0.25, w_recencia_centralidade=0.25
  - Valida lambda_decay ≈ 0.09902
  - Valida que é frozen (tentar alterar campo deve lançar FrozenInstanceError)

TEST 3.2 — ProjectIndex: Auto-Categorização
  - Registra projeto com folder_name "fintech-api"
  - auto_categorize_project() → deve retornar "finanças/quant"
  - Registra projeto "marketing-dashboard"
  - auto_categorize_project() → deve retornar "marketing/vendas"
  - Registra projeto "random-project"
  - auto_categorize_project() → deve retornar "geral" (fallback)

TEST 3.3 — ProjectIndex: Strict Scoping
  - resolve_scope(project_uuid_A, include_references=False) → apenas Primary Wing
  - resolve_scope(project_uuid_A, include_references=True) → Primary + References
  - resolve_scope(project_uuid_A, all_wings=True) → todas as alas

TEST 3.4 — ProjectIndex: Reference Wings
  - get_reference_wings(project_uuid_A) → lista de alas
  - find_similar_projects(project_uuid_A) → projetos da mesma ala

TEST 3.5 — HybridSearchEngine: Pipeline Tri-Sinal
  - Busca "trading engine risk calculation"
  - Valida: retorna resultados com score_final > 0
  - Valida: score_breakdown contém vetorial, frequencia, recencia, centralidade
  - Valida: resultados ordenados DESC por score_final
  - Busca com node_type="FACT" → filtra apenas FACT

TEST 3.6 — GrafoConcierge (Fachada): register_project()
  - Registra novo projeto → retorna UUID válido
  - Registra mesmo folder_name → retorna UUID existente (idempotente)

TEST 3.7 — GrafoConcierge: mine()
  - gc.mine(uuid_A, path_A) → delega ao IngestionManager
  - Valida resultado tem nodes_created

TEST 3.8 — GrafoConcierge: hybrid_search()
  - gc.hybrid_search("trading", project_uuid_A) → retorna lista
  - gc.hybrid_search("trading", project_uuid_A, node_type="FACT")
  - gc.hybrid_search("trading", project_uuid_A, all_wings=True)

TEST 3.9 — GrafoConcierge: commit_memory()
  - gc.commit_memory(uuid_A, "build", "Refatorou trading.py", ["src/trading.py"])
  - Valida: retorna commit_id > 0
  - Verifica que get_recent_commits retorna o novo commit

TEST 3.10 — GrafoConcierge: wake_up()
  - gc.wake_up(uuid_A) → retorna dict com project, resume, reference_wings, etc.
  - Valida que recent_commits contém o commit da 3.9

TEST 3.11 — GrafoConcierge: get_resume()
  - gc.get_resume(uuid_A) → retorna string não vazia

TEST 3.12 — GrafoConcierge: lazy_load()
  - Pega um node_id existente
  - gc.lazy_load(node_id) → retorna dict com edges_out
  - Valida que last_accessed foi atualizado

TEST 3.13 — GrafoConcierge: status()
  - gc.status(uuid_A) → retorna dict com project, stats, reference_wings

TEST 3.14 — GrafoConcierge: find_similar()
  - gc.find_similar(uuid_A) → lista de projetos similares

TEST 3.15 — GrafoConcierge: delete_project()
  - gc.delete_project(uuid_C) → remove vault-secrets
  - Valida: get_project(uuid_C) lança exceção
  - Valida: nós do projeto C foram removidos
  - Valida: vetores do ChromaDB foram limpos

CRITÉRIOS DE SUCESSO: 100% dos testes devem passar.

================================================================================
DIMENSÃO 4: AGENTS (RevisorCritico)
================================================================================

Testa o Guardião de Evolução em todos os cenários.

TEST 4.1 — Auditoria: Commit Válido
  - audit({"technical_changes": "Refatorou módulo X...", "updated_pointers": ["a.py"]})
  - Valida: approved=True

TEST 4.2 — Auditoria: Commit Vazio
  - audit({"technical_changes": "", "updated_pointers": []})
  - Valida: approved=False, reason contém "vazio"

TEST 4.3 — Auditoria: Commit Muito Curto
  - audit({"technical_changes": "fix", "updated_pointers": ["a.py"]})
  - Valida: approved=False, reason contém "curto"

TEST 4.4 — Auditoria: Sem Ponteiros
  - audit({"technical_changes": "Mudança completa no módulo", "updated_pointers": []})
  - Valida: approved=False

TEST 4.5 — Auditoria: Ponteiros Inválidos (strings vazias)
  - audit({"technical_changes": "Mudança completa", "updated_pointers": ["", "  "]})
  - Valida: approved=False

TEST 4.6 — audit_with_retry: Loop de 3 Tentativas → partial_audit
  - Cria generate_fn que sempre retorna draft inválido
  - audit_with_retry(draft_ruim, generate_fn)
  - Valida: approved=True, partial_audit=True, loop_count=3

TEST 4.7 — audit_with_retry: Aprovação na 2ª Tentativa
  - Cria generate_fn que retorna draft válido na 2ª chamada
  - Valida: approved=True, partial_audit=False, loop_count=2

TEST 4.8 — Reranking: Filtragem Heurística
  - Cria 5 candidatos fake com scores variados (0.95, 0.80, 0.60, 0.20, 0.05)
  - rerank(candidates, "buscar trading") → filtra os com score muito baixo
  - Valida: resultado tem menos candidatos que input
  - Valida: garante pelo menos 1 resultado

TEST 4.9 — Reranking: Lista Vazia
  - rerank([], "qualquer") → retorna []

TEST 4.10 — Barreira de Contaminação: RESTRICTED → PUBLIC (BLOQUEIO)
  - check_contamination(source={privacy:RESTRICTED}, target={privacy:PUBLIC})
  - Valida: is_safe=False, reason contém "CONTAMINAÇÃO"

TEST 4.11 — Barreira de Contaminação: PUBLIC → INTERNAL (OK)
  - check_contamination(source={privacy:PUBLIC}, target={privacy:INTERNAL})
  - Valida: is_safe=True

TEST 4.12 — Barreira de Contaminação: INTERNAL → RESTRICTED (OK)
  - check_contamination(source={privacy:INTERNAL}, target={privacy:RESTRICTED})
  - Valida: is_safe=True

TEST 4.13 — Barreira de Contaminação: RESTRICTED → INTERNAL (BLOQUEIO)
  - check_contamination(source={privacy:RESTRICTED}, target={privacy:INTERNAL})
  - Valida: is_safe=False

CRITÉRIOS DE SUCESSO: 100% dos testes devem passar.

================================================================================
DIMENSÃO 5: INTERFACE — MCP SERVER (GrafoConciergeServer)
================================================================================

Testa que o MCP Server foi refatorado corretamente.

TEST 5.1 — Instanciação
  - GrafoConciergeServer(concierge=gc) → não lança exceção
  - Valida: server._gc é a instância de GrafoConcierge
  - Valida: server._mcp é instância de FastMCP

TEST 5.2 — Contagem de Tools
  - Inspeciona server._mcp → deve ter exatamente 8 tools registradas
  - Nomes: concierge_register, concierge_mine, concierge_search,
    concierge_commit, concierge_wakeup, concierge_resume,
    concierge_load, concierge_status

TEST 5.3 — Handler: _handle_register()
  - Chama diretamente server._handle_register("/projetos/test", "geral", "PUBLIC", None)
  - Valida: success=True, project_uuid é string válida

TEST 5.4 — Handler: _handle_search()
  - Chama server._handle_search("trading", uuid_A, 5, None, False, False)
  - Valida: success=True, results é lista

TEST 5.5 — Handler: _handle_commit()
  - Chama server._handle_commit(uuid_A, "build", "Mudança...", ["a.py"], None)
  - Valida: success=True, commit_id > 0

TEST 5.6 — Handler: _handle_wakeup()
  - Chama server._handle_wakeup(uuid_A)
  - Valida: success=True, retorno tem "project" e "resume"

TEST 5.7 — Handler: _handle_resume()
  - Chama server._handle_resume(uuid_A)
  - Valida: success=True, resume é string

TEST 5.8 — Handler: _handle_load()
  - Pega node_id existente
  - Chama server._handle_load(node_id)
  - Valida: success=True, node é dict

TEST 5.9 — Handler: _handle_status()
  - Chama server._handle_status(uuid_A)
  - Valida: success=True, components.sqlite.status == "healthy"

TEST 5.10 — Handler: Erro Gracioso
  - Chama server._handle_wakeup("uuid-inexistente")
  - Valida: success=False, error contém mensagem

CRITÉRIOS DE SUCESSO: 100% dos testes devem passar.

================================================================================
DIMENSÃO 6: INTERFACE — ACTION HOOKS
================================================================================

Testa os gatilhos reativos de ciclo de vida.

TEST 6.1 — on_planning()
  - hooks.on_planning(uuid_A, "implementar autenticação JWT")
  - Valida: retorno tem wake_up, relevant_nodes, task
  - Valida: wake_up tem project e resume
  - Valida: relevant_nodes é lista (pode estar vazia se não houver match)

TEST 6.2 — on_execution()
  - hooks.on_execution(uuid_A, "buscar módulo de trading", rerank=True)
  - Valida: retorna lista de dicts
  - Se houver resultados, valida que foram rerankados

TEST 6.3 — on_execution() sem Reranking
  - hooks.on_execution(uuid_A, "buscar qualquer coisa", rerank=False)
  - Valida: retorna lista (sem filtro do revisor)

TEST 6.4 — on_done() com Commit Válido
  - hooks.on_done(uuid_A, {
      phase: "build",
      technical_changes: "Implementou autenticação JWT com refresh tokens",
      updated_pointers: ["src/auth.py", "src/middleware.py"],
    })
  - Valida: commit_id > 0 (ou None se rejeitado)
  - Valida: audit.approved é bool

TEST 6.5 — on_done() com Trajetória Episódica
  - hooks.on_done(uuid_A, {
      phase: "build",
      technical_changes: "Tentou implementar cache Redis",
      updated_pointers: ["src/cache.py"],
      erro_encontrado: "ConnectionRefusedError: Redis não disponível",
      solucao_aplicada: "Fallback para cache in-memory com TTL",
    })
  - Valida: trajectory_id é int (trajetória criada)

TEST 6.6 — on_done() com Commit Inválido
  - hooks.on_done(uuid_A, {
      phase: "build",
      technical_changes: "",
      updated_pointers: [],
    })
  - Valida: commit_id é None (rejeitado pelo revisor)
  - Valida: audit.approved == False

CRITÉRIOS DE SUCESSO: 100% dos testes devem passar.

================================================================================
DIMENSÃO 7: INTERFACE — CLI
================================================================================

Testa o parser de argumentos e estrutura da CLI.

TEST 7.1 — Parser: Todos os Subcomandos Existem
  - Valida que COMMAND_MAP tem exatamente 9 chaves
  - Chaves: register, mine, search, wakeup, resume, commit, load, status, projects

TEST 7.2 — Parser: Argumentos de "mine"
  - parse_args(["mine", "--path", "/tmp", "--name", "test"])
  - Valida: args.command="mine", args.path="/tmp", args.name="test"

TEST 7.3 — Parser: Argumentos de "search"
  - parse_args(["search", "--query", "JWT", "--project", "abc", "--top-k", "5"])
  - Valida: args.top_k == 5

TEST 7.4 — Parser: Argumentos de "commit"
  - parse_args(["commit", "--project", "abc", "--phase", "build",
    "--changes", "...", "--pointers", "a.py,b.py"])
  - Valida todos os campos

TEST 7.5 — Parser: Flag --no-tag em "mine"
  - parse_args(["mine", "--path", "/tmp", "--name", "t", "--no-tag"])
  - Valida: args.no_tag == True

TEST 7.6 — Parser: Flags booleanas em "search"
  - parse_args(["search", "--query", "x", "--project", "y", "--refs", "--all-wings"])
  - Valida: args.refs == True, args.all_wings == True

CRITÉRIOS DE SUCESSO: 100% dos testes devem passar.

================================================================================
DIMENSÃO 8: MANUTENÇÃO + DECAIMENTO (Janitor + Amnésia)
================================================================================

(Mesma cobertura do memory_stress_test.py original, adaptada para v2)

TEST 8.1 — Janitor: Manutenção após deleção de arquivo
  - Deleta um arquivo do filesystem
  - Re-mine
  - janitor.run_maintenance() → limpa vetores órfãos
  - Valida: orphan_vectors_removed > 0

TEST 8.2 — Janitor: FTS Rebuild
  - fts_rebuild() não lança exceção

TEST 8.3 — Decaimento: Envelhecimento de Nó
  - Seta last_accessed de um nó para 90 dias atrás (via SQL direto)
  - janitor.run_maintenance()
  - Valida: nó migrou para ARCHIVED

TEST 8.4 — Decaimento: Nós Recentes Preservados
  - Valida que nós com last_accessed recente continuam ACTIVE

CRITÉRIOS DE SUCESSO: 100% dos testes devem passar.

================================================================================
RELATÓRIO FINAL
================================================================================

Ao final, imprime relatório visual com:
  - Lista de todos os testes com ✅ ou ❌
  - Contagem: X/Y testes passaram (Z%)
  - Se 100%: "🏆 ABSOLUTE SOLIDITY CONFIRMADA — Pronto para produção"
  - Se < 100%: "❌ N teste(s) falharam — investigar antes de deploy"
  - Tempo total de execução

================================================================================
CONTAGEM TOTAL DE TESTES
================================================================================

Dimensão 1 (Storage):           ~14 testes
Dimensão 2 (Ingestão):          ~6 testes
Dimensão 3 (Core):              ~15 testes
Dimensão 4 (Agents):            ~13 testes
Dimensão 5 (MCP Server):        ~10 testes
Dimensão 6 (Action Hooks):      ~6 testes
Dimensão 7 (CLI):               ~6 testes
Dimensão 8 (Janitor/Decay):     ~4 testes
─────────────────────────────────
TOTAL:                          ~74 testes

================================================================================
CLEANUP
================================================================================

Ao final (no bloco finally):
  - store.close()
  - shutil.rmtree(_stress_test_v2_workspace/)
  - Nenhum resíduo fica no disco
